export type CommonConfig = {
    style: {
        strokeColor: string;
        bgColor: string;
    };
    canvasWidth: number;
    canvasHeight: number;
    watermarkText: string;
    showWatermark: boolean;
}